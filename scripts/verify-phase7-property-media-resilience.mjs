import fs from 'node:fs'; import path from 'node:path'; const r=f=>fs.readFileSync(path.join(process.cwd(),f),'utf8'); const must=(t,n,l)=>{if(!t.includes(n)) throw new Error(`${l}: missing ${n}`)}; const app=r('src/app.ts'), q=r('src/app/module/operationsQueue/operationsQueue.service.ts'), w=r('src/app/module/cron/phase3.worker.ts'), b=r('src/app/module/backup/databaseBackup.service.ts'), d=r('src/app/module/backup/databaseBackup.drill.ts'); for(const x of ['lastDatabaseBackupAt','propertyDraftCleanup','propertyMedia']) must(app,x,'health'); for(const x of ['pendingAssetFinalizationCount','uploadFailuresSinceStart','property_media_upload_failures_total']) must(q,x,'media operations'); must(w,'lastSuccessfulDraftCleanupAt','draft cleanup'); must(b,'assertBackupDatabaseEmpty','backup'); must(d,'BACKUP_DRILL_CONFIRM','drill'); console.log('Phase 7 property media resilience verification passed.')
